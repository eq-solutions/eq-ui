---
"@eq-solutions/ui": minor
---

Add `MultiSelect` — trigger + popover checklist for picking a set of discrete values (a Status/Kind column filter, a form field assigning multiple licences to a worker, and so on). Chips for 1-2 selections, collapses to a count for 3+, search box appears automatically past `searchThreshold` (default 8) options, density-aware. Standalone component — doesn't share code with Table's own filter popover.
