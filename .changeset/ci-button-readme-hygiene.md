---
"@eq-solutions/ui": patch
---

CI now runs `check:barrel` alongside `check:tokens` (so missing component CSS imports cannot ship). Added Button tests locking the #56 loading accessible-name fix. README Install/Setup corrected: publish via GitHub Packages, and `@eq-solutions/tokens` is a peerDependency (not auto-installed).
