---
"@eq-solutions/ui": minor
---

EmptyState: add `variant` prop (`filtered` | `error` | `no-access`) with default icons and, for `error`, a red icon tone. Defaults to `'default'`, which renders exactly as before — no icon unless one is passed. An explicit `icon` prop always overrides the variant's default.
