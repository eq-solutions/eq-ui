---
"@eq-solutions/ui": patch
---

Replace three hardcoded `rgba()` colour literals in `DropdownMenu.css` with canonical `--eq-*` tokens (border, hover background, separator), clearing the token-guard CI gate.
