---
"@eq-solutions/ui": minor
---

Add Spinner component — indeterminate CSS spinner for in-flight action states (form submit, save, mutation). Three sizes: sm (14px, for buttons), md (20px, default), lg (32px, page-level). Inverted variant for dark/primary backgrounds. All colours reference `--eq-*` tokens.
